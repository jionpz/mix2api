services:
  mix2api:
    build:
      context: .
    container_name: mix2api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    ports:
      - "3001:3001"
